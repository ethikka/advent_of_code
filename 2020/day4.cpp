#include <sstream>
#include "../common/lib.h"

std::pair<int,int> solve() {
  std::pair<int,int> res;
  std::string line;
  while (std::cin >> line) {
  }
  return res;
}

int main(void) {
  post_solve(solve());
}
